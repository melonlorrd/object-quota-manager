apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: objectquotas.quota.melonlorrd.dev
spec:
  group: quota.melonlorrd.dev
  names:
    kind: ObjectQuota
    listKind: ObjectQuotaList
    plural: objectquotas
    singular: objectquota
    shortNames:
      - oq
  scope: Namespaced
  versions:
    - name: v1alpha1
      served: true
      storage: true
      schema:
        openAPIV3Schema:
          type: object
          properties:
            spec:
              type: object
              properties:
                fileDescriptors:
                  type: integer
                  format: int64
                processes:
                  type: integer
                  format: int64
                leaseBatchSize:
                  type: integer
                  format: int64
            status:
              type: object
              properties:
                conditions:
                  type: array
                  items:
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
